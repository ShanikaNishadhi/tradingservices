version: '3.8'

services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    container_name: timescaledb
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - tsdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:latest
    container_name: redis
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "127.0.0.1:6379:6379"
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}

  new_coin_listing:
    build:
      context: .
      dockerfile: App/new_coin_listing/Dockerfile
    container_name: new_coin_listing
    restart: always
    network_mode: host
    depends_on:
      - timescaledb
      - redis
    env_file:
      - .env
    volumes:
      - ./App/new_coin_listing:/app
    environment:
      - POSTGRES_HOST=localhost
      - REDIS_HOST=localhost
      - REDIS_PASSWORD=${REDIS_PASSWORD}

  pnlgap:
    build:
      context: .
      dockerfile: App/pnlgap/Dockerfile
    container_name: pnlgap
    restart: always
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/pnlgap:/app/App/pnlgap
      - ./App/helpers:/app/App/helpers

  simpletrends:
    build:
      context: .
      dockerfile: App/simpletrends/Dockerfile
    container_name: simpletrends
    restart: always
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/simpletrends:/app/App/simpletrends
      - ./App/helpers:/app/App/helpers

  breakeven:
    build:
      context: .
      dockerfile: App/breakeven/Dockerfile
    container_name: breakeven
    restart: always
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/breakeven:/app/App/breakeven
      - ./App/helpers:/app/App/helpers

  simpletrends-memecoins:
    build:
      context: .
      dockerfile: App/simpletrends-memecoins/Dockerfile
    container_name: simpletrends-memecoins
    restart: always
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/simpletrends-memecoins:/app/App/simpletrendsmemecoins
      - ./App/helpers:/app/App/helpers

  advancedpnl:
    build:
      context: .
      dockerfile: App/advancedpnl/Dockerfile
    container_name: advancedpnl
    restart: always
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/advancedpnl:/app/App/advancedpnl
      - ./App/helpers:/app/App/helpers

  advancedpnl_backup:
    build:
      context: .
      dockerfile: App/advancedpnl_backup/Dockerfile
    container_name: advancedpnl_backup
    restart: "no"
    depends_on:
      - redis
      - timescaledb
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - ./App/advancedpnl_backup:/app/App/advancedpnl_backup
      - ./App/helpers:/app/App/helpers
    profiles:
      - backup

volumes:
  tsdata:
    external: true
